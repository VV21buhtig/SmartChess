<UserControl x:Class="SmartChess.Views.UserControls.ChessBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SmartChess.Views.UserControls"
             xmlns:components="clr-namespace:SmartChess.Views.Components"
             xmlns:converters="clr-namespace:SmartChess.Resources.Converters"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="480">

    <UserControl.Resources>
        <converters:StringToImageSourceConverter x:Key="StringToImageConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5" Background="LightGray">
            <Button Content="Новая игра" Command="{Binding StartNewGameCommand}" Margin="5" Padding="10,5"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="10,0"/>
        </StackPanel>
        
        <!-- Шахматная доска -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding BoardSquares}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="8" Rows="8"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <components:ChessSquare
                        BackgroundColor="{Binding BackgroundColor}"
                        ImagePath="{Binding PieceImagePath, Converter={StaticResource StringToImageConverter}}"
                        Command="{Binding DataContext.SelectPieceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding Position}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>